# policy-guard Weak Output Anti-Example

Request: plan an operational automation change with rollback and dry-run evidence before enabling it

Weak answer:

This looks like `policy-guard`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `public automation repo with GitHub Actions, scheduled jobs, rollback docs, and dry-run mode` was inspected.
- No symbol such as `main` was confirmed.
- No proof surface was named for `dry run`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
