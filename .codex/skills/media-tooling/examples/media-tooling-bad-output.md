# media-tooling Weak Output Anti-Example

Request: review public docs for overclaim, broken localization, and missing evidence labels

Weak answer:

This looks like `media-tooling`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `public docs repo with README, localized docs, static GitHub catalog, and encoding tests` was inspected.
- No symbol such as `test_readme_files_are_utf8_without_mojibake` was confirmed.
- No proof surface was named for `utf-8`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
