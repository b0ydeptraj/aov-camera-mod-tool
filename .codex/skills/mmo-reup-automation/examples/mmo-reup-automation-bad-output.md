# mmo-reup-automation Weak Output Anti-Example

Request: review reup workflow with source inventory, publish queue, dedupe, reject drawer, and evidence timeline before automation changes

Weak answer:

This looks like `mmo-reup-automation`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `operator-owned reup repo with source inventory, publish queue, dedupe controls, reject drawer, and evidence timeline` was inspected.
- No symbol such as `SourceInventory` was confirmed.
- No proof surface was named for `source inventory`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
