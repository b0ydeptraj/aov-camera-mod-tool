# mmo-browser-fleet-automation Battle-Calibrated Output

Request: review browser fleet automation for profile-to-proxy affinity, session lease, live debug, screenshot, and console log evidence

Recommended skill: `mmo-browser-fleet-automation` because the request matches `mmo-browser-automation` work and has concrete repo anchors.

Read first:

- `ops/browser_profiles.json`
- `workers/browser_session.ts`
- `artifacts/session-screenshot.md`

Evidence gathered:

- Confirmed `SessionLease` or nearby ownership before recommending changes.
- Checked `session lease` and `profile-to-proxy affinity` against the relevant source path.
- Identified `console logs` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `operator ledger` remains unverified until the focused gate or benchmark hit is captured.
